<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".StartActivity">

    <!--
    <item
        android:id="@+id/actionSort"
        android:icon="@mipmap/ic_sort_white_24dp"
        android:title="@string/actionSort"
        app:showAsAction="ifRoom"/>
    -
    <item
        android:id="@+id/actionFilter"
        android:icon="@mipmap/ic_filter_list_white_24dp"
        android:title="@string/actionFilter"
        app:showAsAction="ifRoom"/>
    -->

    <!-- Settings, Logout, should always be in the overflow -->
    <item android:id="@+id/action_logout"
        android:title="@string/action_logout"
        android:icon="@drawable/ic_add_white_24dp"
        app:showAsAction="never" />

    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        app:showAsAction="never"/>

</menu>